import { productBacklog } from './product-backlog';
import { role } from './role';
import { scrum } from './scrum';
import { sprintGuide } from './sprint-guide';
import { sprintPoint } from './sprint-point';

export const introductionController = {
  productBacklog,
  role,
  scrum,
  sprintGuide,
  sprintPoint,
};

export default introductionController;
